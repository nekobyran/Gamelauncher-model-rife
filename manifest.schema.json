{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://gamelauncher.invalid/schemas/distribution-manifest-v1.json",
  "title": "GameLauncher downloadable component manifest",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "id",
    "kind",
    "version",
    "artifacts"
  ],
  "properties": {
    "schemaVersion": {
      "const": 1
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
    },
    "kind": {
      "enum": [
        "engine",
        "model",
        "shell"
      ]
    },
    "version": {
      "type": "string",
      "minLength": 1
    },
    "artifacts": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "platform",
          "abi",
          "format",
          "url",
          "sha256",
          "sizeBytes",
          "maxInstalledBytes",
          "fileName"
        ],
        "properties": {
          "platform": {
            "enum": [
              "android",
              "windows",
              "linux",
              "macos",
              "ios"
            ]
          },
          "abi": {
            "type": "string",
            "minLength": 1
          },
          "format": {
            "enum": [
              "zip",
              "apk",
              "raw"
            ]
          },
          "url": {
            "type": "string",
            "format": "uri"
          },
          "sha256": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$"
          },
          "sizeBytes": {
            "type": "integer",
            "minimum": 1
          },
          "maxInstalledBytes": {
            "type": "integer",
            "minimum": 1
          },
          "fileName": {
            "type": "string",
            "pattern": "^[^/\\\\]+$"
          }
        }
      }
    }
  }
}
